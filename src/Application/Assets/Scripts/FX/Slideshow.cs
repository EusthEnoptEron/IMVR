using UnityEngine;
using System.Collections;

[RequireComponent(typeof(AtlasQuad))]
public class Slideshow : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
